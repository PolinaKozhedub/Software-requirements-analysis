@startuml
title Диаграмма деятельности: Регистрация готовой продукции

|Диспетчер цеха|
start

repeat
    :Открывает форму "Регистрация готовой продукции";
    :Вводит серийный номер и выбирает категорию изделия;
    :Нажимает кнопку "Зарегистрировать";
    
    |Система|
    if (Серийный номер уже существует в БД?) then (да)
        :Выводит ошибку "Дубликат серийного номера";
        |Диспетчер цеха|
        :Корректирует номер;
        note right: Идет на повторную отправку
    else (нет)
        |Система|
        if (Категория == "Автобусы"?) then (да)
            :Требует заполнения поля "Вместимость";
        else if (Категория == "Сельхозмашины"?) then (да)
            :Требует заполнения поля "Производительность";
        endif
        
        :Сохраняет запись со статусом "Сборка";
        :Выводит сообщение "Успешно зарегистрировано";
        stop
    endif
repeat while (Требуется повторный ввод?)
@enduml