@startuml
title Диаграмма деятельности: Регистрация результатов испытаний

|Лаборант|
start
:Выбирает готовое изделие из списка на тестирование;
:Выбирает задействованное оборудование лаборатории;
:Вносит результаты тестов и нажимает "Утвердить протокол";

|Система|
if (Оборудование числится на балансе данной лаборатории?) then (нет)
    :Выводит ошибку "Выбранное оборудование недоступно";
else (да)
    :Записывает протокол испытаний в базу данных;
    if (Результат тестов == "Успешно"?) then (да)
        :Меняет статус изделия на "Готов к отгрузке";
    else (нет)
        :Меняет статус изделия на "Брак / На доработку";
    endif
    :Выводит форму протокола для печати;
endif
stop
@enduml