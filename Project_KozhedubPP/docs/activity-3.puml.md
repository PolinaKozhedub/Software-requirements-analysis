@startuml
title Диаграмма деятельности: Формирование состава бригады

|Сотрудник ОК|
start

repeat
    :Открывает модуль "Управление бригадами";
    :Создает новую бригаду и выбирает сотрудников для добавления;
    :Нажимает "Сохранить состав";

    |Система|
    if (Среди выбранных сотрудников есть персонал типа ИТР?) then (да)
        :Блокирует сохранение;
        :Выводит ошибку "В бригаду можно добавлять только Рабочих";
        |Сотрудник ОК|
        :Удаляет ИТР из списка;
        note right: Необходим повторный клик "Сохранить"
    else (нет)
        |Система|
        :Назначает бригадира из числа выбранных рабочих;
        :Сохраняет состав бригады в базе кадров;
        :Выводит подтверждение операции;
        stop
    endif
repeat while (Требуется повторное сохранение?)
@enduml